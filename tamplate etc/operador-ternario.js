// If ... else de forma mais bonita e simples

//idade >= 18 ? 'Maior de idade' : 'Menor de idade'

function calculaIdade(idade) {
  return idade >= 18 ? 'Maior de idade' : 'Menor de idade'
}
console.log(calculaIdade(19))
